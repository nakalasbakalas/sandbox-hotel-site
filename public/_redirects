# Marketing-site redirects for booking and staff entry points are handled
# in the Cloudflare Worker at src/index.js so they can use environment-specific
# canonical origins. Keep this file for static-only redirects if needed later.
