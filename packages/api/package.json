{
  "name": "@sandbox-hotel/api",
  "version": "1.0.0",
  "description": "Sandbox Hotel API and worker functions",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir=dist"
  },
  "dependencies": {
    "@sandbox-hotel/shared": "*"
  },
  "devDependencies": {
    "wrangler": "^4.72.0"
  }
}
