{
  "name": "@sandbox-hotel/web",
  "version": "1.0.0",
  "description": "Sandbox Hotel website frontend",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "test:booking-links": "playwright test tests/booking-cta.spec.js --reporter=line"
  },
  "dependencies": {
    "@sandbox-hotel/shared": "*"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "wrangler": "^4.72.0"
  }
}
