{
  "name": "sandbox-hotel-site",
  "version": "1.0.0",
  "description": "Hotel site hosted on Cloudflare Workers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler build"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.92.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
